repos:
-   repo: https://github.com/ambv/black
    rev: stable
    hooks:
    - id: black
      args: [--line-length=80, --safe]
      python-version: python3.6
      files: ^(uplink/.*|setup|tests/.*|examples/.*|docs/conf).py$
